<div class="list-form">
  <%= simple_form_for @list_new do |f| %>
    <%= f.input :name %>
    <%= f.input :photo, as: :file %>
    <%= f.submit 'Submit', class: 'btn submit-btn' %>
  <% end %>
</div>
